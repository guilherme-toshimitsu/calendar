import React from 'react';
import PropTypes from 'prop-types';

const Home = ({ history }) => {
  
  return (
    <>
      <div>
        {'Home 2'}
      </div>
    </>
  );
};

Home.propTypes = {
  history: PropTypes.object,
};

export default Home;
