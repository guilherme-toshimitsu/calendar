const styles = (theme) => ({
  root: {
    maxWidth: 1360,
    width: '70%',
    margin: 'auto',
    padding: 40,
  },
  title: {
    textAlign: 'left',
    fontSize: 24,
    color: theme.mainStyle.mainThemeColor,
    fontStyle: 'bold',
  },
  text: {
    textAlign: 'left',
    fontSize: 16,
    marginTop: 16,
  },
  subContainer: {
    marginTop: 32,
    marginBottom: 32,
  },
});

export default styles;
