const styles = (theme) => ({
	root: {
		position: 'relative',
		textAlign: 'center',
		minHeight: '100vh',
		minWidth: 320,
		overflow: 'hidden',
		backgroundColor: theme.mainStyle.mainBackgroundColor,
	},
});

export default styles;
