export const UPDATE_SESSION = Symbol('refresh session data'),
  SYNC_SESSION = Symbol('sync session data'),
  LOGIN = Symbol('Login'),
  LOGOUT = Symbol('Logout'),
  CHECK_TOKEN = Symbol('get Token validity');
