import React from 'react';

const Default2 = ({ onClose }) => {

  return (
    <div >
      <button onClick={() => onClose()}>fechar</button>
      Default Modal 22222
    </div>
  );
};

export default Default2;
